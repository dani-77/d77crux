# Description: Simple GUI application that writes compressed disk images to USB drives and creates backups
# URL: https://bztsrc.gitlab.io/usbimager/
# Maintainer: Daniel Azevedo, daniazevedo77 at posteo dot net
# Depends on:  qt-sudo

name=usbimager
version=1.0.10
release=3
source=(https://gitlab.com/bztsrc/$name/-/archive/$version/$name-$version.tar.gz)

build() {
	cd $name-$version/src
	make
	make DESTDIR=$PKG install
}
